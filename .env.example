# =============================================================================
# ZapManejo Backend Environment Configuration
# =============================================================================
# IMPORTANT: This is a template file. Copy to .env and fill with your values.
# NEVER commit the actual .env file with real credentials to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (DigitalOcean PostgreSQL)
# -----------------------------------------------------------------------------
# Get from: DigitalOcean Console > Databases > Your DB > Connection Details
# Format: postgres://username:password@host:port/database?sslmode=require
DATABASE_URL=postgres://doadmin:YOUR_PASSWORD@YOUR_HOST.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for local development (DigitalOcean will override with PORT=8080)
PORT=5000

# -----------------------------------------------------------------------------
# Security - JWT Authentication
# -----------------------------------------------------------------------------
# CRITICAL: Generate a strong random secret (minimum 32 characters)
# Generate with: openssl rand -base64 32
# Or use: https://www.random.org/strings/
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_32_CHARS_MIN

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Allowed frontend origins (comma-separated for multiple)
# Local development example: http://localhost:3000
# Production example: https://zapmanejo.com,https://www.zapmanejo.com
ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# PayPal Integration
# -----------------------------------------------------------------------------
# Get from: https://developer.paypal.com/dashboard/
# Use sandbox credentials for testing, live for production
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_SECRET=your_paypal_secret_here
PAYPAL_MODE=sandbox  # Change to 'live' for production

# -----------------------------------------------------------------------------
# WhatsApp Business Integration
# -----------------------------------------------------------------------------
# Your WhatsApp Business phone number (with country code, no + or spaces)
WHATSAPP_NUMBER=15616520911

# WhatsApp webhook verification token (create a random secure string)
# Generate with: openssl rand -base64 24
WHATSAPP_VERIFY_TOKEN=REPLACE_WITH_RANDOM_TOKEN

# -----------------------------------------------------------------------------
# Business Logic - Lifetime Plan Configuration
# -----------------------------------------------------------------------------
# Total lifetime plan slots available for early adopters
LIFETIME_TOTAL=200

# Current number of lifetime slots sold (tracked in database)
LIFETIME_SOLD=0

# =============================================================================
# DEPLOYMENT NOTES FOR DIGITALOCEAN APP PLATFORM
# =============================================================================
# 1. Do NOT add these values to app.yaml - use the DO Console instead
# 2. Go to: App Settings > Components > backend > Environment Variables
# 3. Add each variable as "encrypted" to protect sensitive data
# 4. Required variables that MUST be set:
#    - DATABASE_URL (from your DO PostgreSQL database)
#    - JWT_SECRET (generate strong random value)
#    - WHATSAPP_VERIFY_TOKEN (generate strong random value)
# 5. PORT is automatically set to 8080 by DigitalOcean App Platform
# =============================================================================
